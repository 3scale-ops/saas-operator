---
apiVersion: secrets-manager.tuenti.io/v1alpha1
kind: SecretDefinition
metadata:
  name: mappingservice-system-master-access-token
  namespace: "{{ ansible_operator_meta.namespace }}"
  labels:
    app: mapping-service
    part-of: 3scale-saas
spec:
  type: opaque
  name: mappingservice-system-master-access-token
  keysMap:
    access-token:
      path: {{ secret.system_admin_token_vault_path }}
      key: access-token
