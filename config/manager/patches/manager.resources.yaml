apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
        - name: manager
          resources:
            requests:
              cpu: 450m
              memory: 256Mi
            limits:
              cpu: 900m
              memory: 1Gi