apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
        - name: manager
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.annotations['olm.targetNamespaces']
          args:
            - operator
            - --leader-elect
          resources:
            limits:
              cpu: 50m
              memory: 700Mi
            requests:
              cpu: 25m
              memory: 200Mi
          ports:
            - containerPort: 8080
              protocol: TCP
              name: metrics
